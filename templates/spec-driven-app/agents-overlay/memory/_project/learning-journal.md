# Learning journal (append-only)

Auto-logged by **SpecForge** plugin hooks when `.specs/` or `agent-memory/` files change.

Distill durable facts into `MEMORY.md` files. Remove noise; specs remain source of truth.
